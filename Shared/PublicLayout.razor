@inherits LayoutComponentBase

<main class="bm-main">
  @Body
</main>

@code {
    // Removed navInit JS interop since Bootstrap collapse handles navbar toggling
    // Keeping JS injection here in case you add theme or other interop later
    [Inject] IJSRuntime JS { get; set; } = default!;
}
